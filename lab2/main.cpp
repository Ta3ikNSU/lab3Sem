#include <iostream>
#include <fstream>
int main(const int argc, const char** argv) {
    /*
    Calculator clk;
    if(argc > 1){
        std::ifstream fin;
        fin.open(argv[1]);
        //if(!fin.is_open())
        std::cout << clk.calculate(fin);
    } else {
        std::cout << clk.calculate(std::cin);
    }
    */
}
